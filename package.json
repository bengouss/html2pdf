{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && esbuild lambda.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "docker-build": "npm run build && docker build --provenance false -t bgrynblat/html2pdf:latest .",
    "docker-dev": "npm run docker-build && docker run --rm -p 9000:8080 -v \"$(pwd)/tmp:/tmp\" bgrynblat/html2pdf:latest",
    "docker-push": "npm run docker-build && docker push bgrynblat/html2pdf:latest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^24.15.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "@aws-crypto/sha256-js": "^5.2.0",
    "@aws-sdk/signature-v4": "^3.370.0",
    "@types/aws-lambda": "^8.10.150",
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "axios": "^1.11.0",
    "esbuild": "^0.25.5",
    "express": "^4.18.2"
  }
}
